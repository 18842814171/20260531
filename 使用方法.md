# myos — 使用方法（操作员）

与 [`docs/README.md`](docs/README.md) 配套的中文速查。路径均相对于仓库根目录。

---

## 1. 构建

```bash
cd code/myos
make              # 内核 DEBUG=1，串口输出 LOG {...}
make DEBUG=0      # 内核日志关闭，交互更干净
make clean && make home && make   # 改过 home/root/*.c 后建议全量重建
```

| 标志 | 含义 |
|------|------|
| `make DEBUG=1` | 内核编译期打开 `LOG_*`（与 shell 的 `DEBUG=` **无关**） |
| `make DEBUG=0` | 内核不输出 `LOG {...}` |
| `make AUTORUN=ipc_echo` | 启动后自动执行 `./ipc_echo` 再关机 |

---

## 2. 启动 QEMU（交互，推荐）

```bash
cd code/myos
make DEBUG=0
DEBUG=n ./sh/start_qemu.sh
```

- **`DEBUG=n`**：串口直连终端，**键盘能进 guest**。
- **`DEBUG=y`**：stdout 管道进 osviz，**不能用来打字进系统**（仅抓 LOG）。

登录：`root`（无密码）。默认目录 `/home/root`。

---

## 3. IPC 演示 `ipc_echo`

```text
root@/home/root$ ./ipc_echo
[sem] create empty=8 full=0 mutex=1
producer pid=3 start
consumer pid=4 start
Type text and press Enter (q to quit):
```

输入 `hello` + Enter → 应看到 `[producer] put` / `[consumer] get` 及字符回显。  
输入 `q` + Enter → producer/consumer 退出，父进程 `waitpid` 返回。

非交互冒烟：

```bash
make clean && make AUTORUN=ipc_echo DEBUG=0
printf 'hello\n' | DEBUG=n timeout 20 qemu-system-riscv64 -nographic -serial stdio \
  -display none -monitor none -smp 1 -machine virt -m 128M \
  -bios firmware/fw_jump -kernel out/os
```

---

## 4. 调试

```bash
cd code/myos
./sh/debug.sh          # GDB + 预设断点（gdb_init）
make AUTORUN=pagefault
./sh/start_qemu.sh
```

---

## 5. Web 前端

```bash
cd code/web
./start_server.sh
```

浏览器访问部署地址（常见 nginx `:3333` 或 Flask `:5000`）。  
Web 会话内 `server.py` 固定 `DEBUG=n`，LOG 与 shell 文本由服务端 demux。

详见 [docs/05_web_frontend.md](docs/05_web_frontend.md)。

---

## 6. 编译用户程序

```bash
cd code/myos
./compile.sh c ipc_echo.c
make                     # 重新打包 ramfs
```

---

## 7. 常见问题

| 现象 | 处理 |
|------|------|
| 按键无反应 | 用 `DEBUG=n ./sh/start_qemu.sh`，勿用 `DEBUG=y` |
| 满屏 `LOG {...}` | `make DEBUG=0` 或接受 DEBUG=1 的诊断输出 |
| `./ipc_echo` 卡住 | 确认已拉最新内核（含 `proc_user_run_dispatch`）；见 [PROBLEMS_AND_SOLUTIONS.md](docs/PROBLEMS_AND_SOLUTIONS.md) §13 |
| Web 终端空、气泡有 | 等串口出现 `Welcome, root.` 后再输入 |

---

## 8. 文档索引

| 文档 | 内容 |
|------|------|
| [docs/README.md](docs/README.md) | 英文总索引 |
| [docs/01_architecture.md](docs/01_architecture.md) | 架构与数据流 |
| [docs/02_call_chains.md](docs/02_call_chains.md) | 调用链 |
| [docs/PROBLEMS_AND_SOLUTIONS.md](docs/PROBLEMS_AND_SOLUTIONS.md) | 问题与修复记录 |

---

*与内核 `proc_sched` block/wakeup、`ipc_echo` 演示路径同步。*
