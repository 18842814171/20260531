

## 1. 整体数据路径

```text
Host 键盘
  → QEMU 进程 stdin（-serial stdio）
  → QEMU 模拟 NS16550 @ 0x10000000
  → guest MMIO 读 LSR/RHR
  → uart_getc() 轮询阻塞
  → uart_read_line()
  → console.c login / shell
```

**TX 路径**（已验证可用）：`uart_putc` → 写 RHR → QEMU → host stdout。

**RX 路径**（当前问题点）：`uart_getc` 读 LSR bit0（RX_READY），为 0 则死循环等待； LSR 诊断一直 `60`（bit0=0），说明 guest 从未看到「有数据可读」。

---

## 2. 硬件与地址

| 项 | 值 |
|---|---|
| UART 基址 | `0x10000000`（`platform.h` 的 `UART0`） |
| 寄存器 | RHR=0, IER=1, FCR=2, LCR=3, LSR=5 |
| LSR bit0 | `UART_LSR_RX_READY`，有 RX 数据时为 1 |
| LSR `0x60` | bit5/6 置位，TX 空闲，**无 RX 数据** |

---

## 3. 初始化（`uart_init` / `uart_irq_enable`）

**启动顺序**（`kernel.c`）：

```text
uart_init()          // 最早，start_kernel 开头
  … page_init / trap / plic / timer / sched …
uart_irq_enable()    // 关 IER，强制轮询
cpu_irq_enable()     // 开全局中断（timer 等）
console_run()        // 开始读键盘
```

**`uart_init`**（`boot/uart.c`）：

- 清 ring：`uart_rx_head/tail = 0`
- `uart_rx_use_irq = 0`（轮询模式）
- 设波特率：DLAB=1 → DLL=3, DLM=0 → DLAB=0, 8N1
- FCR = enable + clear FIFO
- `uart_rx_flush()` 清残留

**`uart_irq_enable`**：

- 保持 `uart_rx_use_irq = 0`
- `UART_IER = 0`（不使能 RX 中断）
- PLIC 也未 enable UART0（`plic.c` 注释说明）

**结论**：当前 **纯轮询**，不走 IRQ ring。

---

## 4. RX 软件缓冲（IRQ 专用，现未启用）

```text
uart_isr()  → uart_rx_put()  → ring[256]
uart_getc() → 先 uart_rx_get() 查 ring
```

- `uart_rx_use_irq == 0` 时，`uart_getc` 跳过 ISR 路径，**直接 poll MMIO**
- ring 只在 `uart_rx_use_irq=1` 且 PLIC+IER 都开时才有意义

---

## 5. 核心读函数（轮询链）

### `uart_getc()` — 唯一阻塞点

```238:267:code/myos/boot/uart.c
int uart_getc(void)
{
	int c;

	for (;;) {
		// ... uart_rx_use_irq 分支（当前不走）...

		c = uart_rx_get();          // ring 空则 -1
		if (c >= 0) { ... return c; }

		while ((uart_read_reg(UART_LSR) & UART_LSR_RX_READY) == 0)
			;                       // ← 卡在这里：等 LSR bit0
		c = uart_read_reg(UART_RHR);
		if (c != 0)
			return c;               // c==0 则丢弃，继续循环
	}
}
```

login 后「敲键无反应」= 程序停在这个 `while (RX_READY == 0)`。

### `uart_read_line()` — 行编辑

- 循环 `uart_getc()`
- `\r`/`\n` 结束行
- 忽略 Ctrl+C（3）、处理退格（8/127）
- 只收可打印 ASCII（32–126）
- **guest 不回显**（设计如此，靠 host `stty echo`）
- 行末 `uart_drain_echo_prefix("\n")` 吞掉 stdio 回环的换行

### `uart_rx_flush` / `uart_rx_flush_deep`

- 清空 ring + 读光 RHR 里已有字节
- `flush_deep` = 连续 8 次 flush
- **会丢弃 flush 期间到达的所有输入**

### `uart_prompt_and_read_line()`

- 打印 prompt → `uart_rx_flush()` → `uart_read_line()`
- shell 提示符用这个；login 没用这个，自己调 flush

---

## 6. Console / Login 调用链

```text
kernel.c: console_run()
  └─ console.c: console_run()
       ├─ uart_puts("=== myos console ===")
       └─ login_session()
            ├─ uart_puts("Username: root\nlogin: \n")
            ├─ uart_rx_flush_deep()      ← 清回环/垃圾
            └─ for (;;)
                 ├─ uart_read_line()    ← 阻塞等键盘
                 ├─ login_name_plausible()  ← 非 a-z 静默丢弃
                 └─ str_eq("root") → 进 shell

       └─ shell_loop()
            └─ uart_prompt_and_read_line("root@...$ ", ...)
```

**login 过滤**（`console.c`）：

- 空行 → `continue`
- 非小写字母 / 长度>8 → **静默 `continue`**（无 "Login incorrect"）
- 只有 plausible 且非 root 才打印 "Login incorrect"

因此：若 RX 收到乱码回环，可能一直空转，看起来像「没反应」。

---

## 7. Host 侧（`start_qemu.sh`）

| 条件 | 行为 |
|---|---|
| 交互终端（`-t 0`） | 默认 `DEBUG=n`，`exec qemu`，`-serial stdio` |
| 非交互（脚本/CI） | 默认 `DEBUG=y`，`qemu \| python serial_reader` |

交互模式：

- `stty icanon echo`：host 回显，按 Enter 才送一行
- guest 不回显，靠 host echo 看到输入

**`-serial stdio` 回环**：guest TX 会回到 guest RX，所以大量 flush / drain 逻辑。

---

## 8. 中断路径（当前关闭）

```text
timer IRQ → trap_handler → schedule 等
UART IRQ  → uart_isr → ring   （未启用）
```

`trap.c` 里 `external_interrupt_handler` 有 `uart_isr`，但 IER=0、PLIC UART 未 enable，**轮询路径与 ISR 不交叉**。

---

## 9. 日志的对应关系

 boot 日志正常到：

```text
Username: root
login:
```

说明：

- TX / MMIO 写：正常
- 已执行 `uart_rx_flush_deep()`
- 已进入 `uart_read_line()` → `uart_getc()` → **等 RX_READY**

之后 `[1]+ Stopped` = 你按了 **Ctrl+Z** 把 shell 脚本挂起，QEMU 进后台，键盘自然无效。这是你主动停程序的方式，不是 login 逻辑本身。

LSR 诊断一直 `60` = **从 guest 视角，host 键盘字节从未进 RHR**（问题在 Host→QEMU→MMIO 这一段，或启动方式，不在 console 过滤逻辑）。

---

## 10. 关键文件索引

| 文件 | 职责 |
|---|---|
| `boot/uart.c` | MMIO、init、getc/read_line、flush、ISR |
| `boot/kernel.c` | 启动顺序、boot 后 flush、进 console |
| `usr/console.c` | login/shell 读行与过滤 |
| `include/platform.h` | `UART0 = 0x10000000` |
| `interrupt/plic.c` | UART IRQ 未 enable |
| `interrupt/trap.c` | `uart_isr` 入口（未走） |
| `sh/start_qemu.sh` | QEMU 串口、host stty、osviz 管道 |

---

## 11. 设计约束（读代码时需记住）

1. **双模式互斥**：IRQ ring 与 poll RHR 不能同时读同一字节  
2. **guest 不回显**：避免 `-serial stdio` TX→RX 回环更严重  
3. **大量 flush**：为清回环，但 flush 窗口内的真实输入也会被丢  
4. **login 静默过滤**：乱码行不报错，表现为空转  
5. **阻塞模型**：无 timeout，`RX_READY` 永不置位则永远卡住  

---

## 12. 问题分层（便于后续排查）

```text
Layer A  Host → QEMU stdin     （终端/管道/Ctrl+Z 挂起）
Layer B  QEMU → guest MMIO RX  （LSR bit0，诊断 60=未到达）
Layer C  uart_getc / read_line （阻塞与过滤）
Layer D  login / shell 逻辑    （plausible 过滤等）
```

当前现象落在 **Layer A 或 B**：Layer C/D 还没机会处理真实按键。