按你的顺序，建议这样排（**可视化放最后**）。

---

## 第一优先级：最小 `fork` / `exec` / `wait`（覆盖三块）

目标：去掉现在的 **`prog_exec` + `shell_save_cxt` 协程模型**，换成「真进程 + 子进程跑 ELF」，shell 只做前台/等待。

### Linux 里和多进程直接相关的命令（只列这一类）

| 命令 | 用途（和你要做的 syscall 对应） |
|------|--------------------------------|
| `ps` / `ps aux` | 看进程列表、状态、父子关系 |
| `top` / `htop` | 动态看谁在跑、占 CPU |
| `kill` / `kill -9 <pid>` | 向进程发信号（后面第三优先级再做） |
| `wait` | shell 里等后台子进程结束（内置，不是独立二进制） |
| `jobs` / `fg` / `bg` | 作业控制（可不做，知道即可） |
| `fork` 演示 | 常写 `fork()` 的小程序，或用 shell 跑 `./a &` 看并发 |
| `execve` 对应用户态 | `./prog`、`exec ls` 这类「换镜像」 |
| `strace -f ./prog` | 宿主机上 trace **fork/exec/wait** 系列 syscall（调试用） |

第一优先级 **不必先实现** `kill`/`pipe`；shell 里先有 **`ps` + `./prog`（走 exec）+ 父进程 wait** 就够对标 task。

### 建议最小实现切片（三块一起改）

**A. 进程管理（`proc/`）**

- 扩展 PCB：`pid/ppid/state`、`pagetable` 指针（第二优先级再填）、`fd 表` 可先共用全局或每进程一份浅拷贝。
- `fork()`：复制 PCB + 复制用户地址空间内容（**第一版可先 flat 复制物理页**，等 Sv39 再改成拷页表）。
- `exec()`：解析 ELF、换用户镜像、清 fd（可选）、**新 argc/argv 先占位**（第三优先级再真传参）。
- `wait()` / `waitpid()`：父进程阻塞或轮询，收子进程 `exit` 状态，子进程变 **zombie → 回收**。
- 与现有 `sched.c` 衔接：用户进程也占一个 `ctx_tasks` 槽，或单独 `user_proc[]`；**先 1 个用户进程 + 1 个 shell 内核线程** 再扩并发。

**B. syscall（`proc/syscall.c` + `trap.c`）**

- 实现：`SYS_fork`(214)、`SYS_execve`（或沿用路径 + 新号)、`SYS_wait4`/`SYS_waitpid`、`SYS_getpid`。
- `SYS_exit`：按 **pid** 结束当前进程，不再走 `prog_exec_done` / `shell_save_cxt`。
- 删除或降级：`prog_exec_active`、`shell_save_cxt` 整条特殊路径。

**C. shell（`usr/console.c`）**

- `./hi` → 内核 **`fork` + 子进程 `exec` + 父进程 `wait`**（或封装 `kernel_spawn(path)` 内部等价）。
- `ps`：遍历 PCB，打印 pid/ppid/state/name（已有 `proc_list` 可接）。
- 保留现有 `ls/cat/...`；**不再**在 shell 栈上 `switch_to(user)`。

**第一优先级验收（最小）**

```text
root@/home/root$ ./hi
hi from ./hi
root@/home/root$ ps
  PID  PPID  STAT  NAME
    1     0  S     shell
root@/home/root$ ./hi &
[1] 2
root@/home/root$ wait
root@/home/root$
```

（`&`/`wait` 可第二步；最小版只要 **前台 fork/exec/wait 一次** 即可。）

---

## 第二优先级：Sv39 页表（不必完整 Demand Paging）

- 每进程独立 `satp`（Sv39），用户区映射在 **低地址**，内核映射在 **高地址**（或 trampoline 页）。
- `fork`：拷页表 + 写时复制可 **先全量拷物理页**。
- `exec`：建新用户映射，丢弃旧用户映射。
- trap 入口：切换 `satp` + 内核栈；`sret` 回用户前再切回用户页表。
- 缺页 handler：**可先 panic 或 stub**，不算本阶段必须。

做到「每进程独立页表 + 用户/内核空间分离」，task 里内存管理完成度会跳一大截。

---

## 第三优先级：`kill` / `pipe` / `argv`

| 项 | 作用 |
|----|------|
| `argv` | `exec` 传参；shell 解析 `line` → `argc/argv` 压用户栈 |
| `pipe` | `pipe` syscall + shell `\|`（可先单向） |
| `kill` | 向 PCB 发「待退出」；shell 命令 `kill <pid>` |

---

## 第四优先级：缺页 / `seek` / `mkfs`

- 缺页：按需映射、栈扩展示意。
- `seek`：RAMFS 文件偏移。
- `mkfs`：宿主机工具或启动时格式化（你现在用 `pack_home` 种子，可文档化等价关系）。

---

## 和当前代码的关系（一句话）

| 现状 | 第一优先级要换掉 |
|------|------------------|
| `prog_exec` + `shell_save_cxt` + `prog_exec_done` | 真 `fork/exec/wait` + 按 pid 的 `exit` |
| `SYS_fork` → `ENOSYS` | 真 fork |
| `ps` 主要是内核 demo worker | 基于 PCB 的真 `ps` |

---

## 建议实施顺序（第一优先级内部）

1. `getpid` + 改 `exit` 按当前进程结束（不动 fork）  
2. `exec` 加载 ELF 到「当前进程」用户区（仍单进程）  
3. `fork` + 父/子两套上下文  
4. `wait` + zombie 回收  
5. shell：`./prog` 改走 fork/exec/wait，删 `prog_exec` 特殊路径  
6. `ps` 接 PCB  
